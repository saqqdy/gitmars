#!/usr/bin/env node
const{program}=require("commander");const{options,args}=require("./conf/build");const{createArgs}=require("./js/tools");const runJenkins=require("./js/runJenkins");program.name("gitm build").usage("<project>").description("构建Jenkins");if(args.length>0)program.arguments(createArgs(args));options.forEach(o=>{program.option(o.flags,o.description,o.defaultValue)});program.action((project,opt)=>{runJenkins({env:opt.env,project:project,app:opt.app})});program.parse(process.argv);