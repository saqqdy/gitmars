#!/usr/bin/env node
"use strict";const{program:e}=require("commander");require("fs");const r=require("path"),s=require("shelljs"),{options:i,args:a}=require("./conf/ui"),{createArgs:t}=require("./js/tools");e.name("gitm ui").usage("<name> <path>").description("链接本地包"),a.length>0&&e.arguments(t(a)),i.forEach((r=>{e.option(r.flags,r.description,r.defaultValue)})),e.action((e=>{process.chdir(r.join(__dirname,"../server")),s.exec("npm run server:start")})),e.parse(process.argv);
