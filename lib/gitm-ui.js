#!/usr/bin/env node
"use strict";const{program:e}=require("commander"),r=require("path"),s=require("shelljs"),{options:a,args:i}=require("./conf/ui"),{createArgs:t}=require("./js/tools");e.name("gitm ui").usage("<name> <path>").description("链接本地包"),i.length>0&&e.arguments(t(i)),a.forEach((r=>{e.option(r.flags,r.description,r.defaultValue)})),e.action((e=>{process.chdir(r.join(__dirname,"../app")),s.exec("npm run server:start")})),e.parse(process.argv);
