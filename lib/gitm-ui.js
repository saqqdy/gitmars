#!/usr/bin/env node
"use strict";var program=require("commander");var fs=require("fs");var path=require("path");var sh=require("shelljs");var _require=require("./conf/ui"),options=_require.options,args=_require.args;var _require2=require("./js/tools"),createArgs=_require2.createArgs;program.name("gitm ui").usage("<name> <path>").description("链接本地包");if(args.length>0)program.arguments(createArgs(args));options.forEach(function(o){program.option(o.flags,o.description,o.defaultValue)});program.action(function(opt){sh.exec("pwd");process.chdir(path.join(__dirname,"../server"));sh.exec("npm run server:start")});program.parse(process.argv);