#!/usr/bin/env node
const{program}=require("commander");const fs=require("fs");const path=require("path");const sh=require("shelljs");const{options,args}=require("./conf/ui");const{createArgs}=require("./js/tools");program.name("gitm ui").usage("<name> <path>").description("链接本地包");if(args.length>0)program.arguments(createArgs(args));options.forEach(o=>{program.option(o.flags,o.description,o.defaultValue)});program.action(opt=>{process.chdir(path.join(__dirname,"../server"));sh.exec(`npm run server:start`)});program.parse(process.argv);