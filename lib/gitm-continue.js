#!/usr/bin/env node
"use strict";var program=require("commander");var sh=require("shelljs");var _require=require("./conf/continue"),options=_require.options,args=_require.args;var _require2=require("./js/index"),error=_require2.error,queue=_require2.queue,getCache=_require2.getCache;var _require3=require("./js/tools"),createArgs=_require3.createArgs;program.name("gitm continue").usage("[-l --list]").description("继续未完成的操作");if(args.length>0)program.arguments(createArgs(args));options.forEach(function(o){program.option(o.flags,o.description,o.defaultValue)});program.action(function(opt){var cmd=getCache();if(opt.list){sh.echo(cmd);sh.exit(0)}if(cmd.length>0){queue(cmd)}else{sh.echo(error("队列里面没有未执行的指令"))}});program.parse(process.argv);